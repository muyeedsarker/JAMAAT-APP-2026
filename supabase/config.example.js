// Copy this file to supabase/config.js and fill in values from
// Supabase Dashboard > Project Settings > API.
// Only use the Project URL and the publishable/anon key in browser code.
// NEVER commit a service_role/secret key.
window.SUPABASE_CONFIG = {
  url: 'https://YOUR-PROJECT-REF.supabase.co',
  anonKey: 'YOUR-PUBLISHABLE-OR-ANON-KEY'
};
